<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FridgeScan.Views.ProductsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviours="clr-namespace:FridgeScan.Behaviours"
    xmlns:converters="clr-namespace:FridgeScan.Converters"
    xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
    xmlns:models="clr-namespace:FridgeScan.Models"
    xmlns:pulltoRefresh="clr-namespace:Syncfusion.Maui.PullToRefresh;assembly=Syncfusion.Maui.PullToRefresh"
    xmlns:sf="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
    xmlns:vm="clr-namespace:FridgeScan.ViewModels"
    xmlns:controls="clr-namespace:FridgeScan.Controls"
    x:Name="ProductsPageRoot"
    x:DataType="vm:ProductsViewModel"
    BackgroundColor="#0D1023">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ExpandCollapseIconConverter x:Key="ExpandCollapseIconConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto">
        <!-- Main content area -->
        <Grid Padding="12,0" Grid.Row="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Large expressive header -->
        <StackLayout Grid.Row="0" Spacing="12" Padding="0,20,0,0">
            <!-- Page header: title + subtitle only (no action buttons) -->
            <VerticalStackLayout Spacing="4" Padding="4,0,4,0">
                <Label Text="Inventory"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="White"
                       CharacterSpacing="-0.3" />
                <Label Text="Keep track of products and expiry dates"
                       FontSize="13"
                       TextColor="#8888AA" />
            </VerticalStackLayout>

            <!-- Action toolbar: Search, Filter, Sort pills -->
            <Grid x:Name="ToolbarGrid"
                  RowDefinitions="Auto,Auto"
                  Margin="4,8,4,12">

                <!-- Row 0: Toolbar pills (collapsed vs expanded) -->
                <Grid Grid.Row="0" x:Name="ToolbarRow" HeightRequest="36">

                    <!-- Collapsed: icon-only search + auto-sized filter/sort -->
                    <HorizontalStackLayout x:Name="CollapsedToolbar"
                                            Spacing="8"
                                            VerticalOptions="Center">
                        <!-- Search icon button (no text) -->
                        <Border x:Name="SearchPill"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                WidthRequest="36"
                                HeightRequest="36"
                                Padding="0">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSearchPillTapped" />
                            </Border.GestureRecognizers>
                            <Label Text="&#xe8b6;"
                                   FontFamily="Material"
                                   FontSize="16"
                                   TextColor="#8888AA"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>

                        <!-- Filter pill (auto-width) -->
                        <Border x:Name="FilterPill"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                HeightRequest="36"
                                Padding="14,0">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnFilterPillTapped" />
                            </Border.GestureRecognizers>
                            <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                <Label x:Name="FilterIcon"
                                       Text="&#xe152;"
                                       FontFamily="Material"
                                       FontSize="14"
                                       TextColor="#CCCCDD"
                                       VerticalOptions="Center" />
                                <Label x:Name="FilterLabel"
                                       Text="Filter"
                                       FontSize="12"
                                       TextColor="#CCCCDD"
                                       VerticalOptions="Center" />
                                <Border x:Name="FilterDot"
                                        IsVisible="False"
                                        WidthRequest="6"
                                        HeightRequest="6"
                                        BackgroundColor="#D0BCFF"
                                        StrokeShape="RoundRectangle 3"
                                        Stroke="Transparent"
                                        VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Border>

                        <!-- Sort pill (auto-width) -->
                        <Border x:Name="SortPill"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                HeightRequest="36"
                                Padding="14,0">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSortPillTapped" />
                            </Border.GestureRecognizers>
                            <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                <Label x:Name="SortIcon"
                                       Text="&#xe164;"
                                       FontFamily="Material"
                                       FontSize="14"
                                       TextColor="#CCCCDD"
                                       VerticalOptions="Center" />
                                <Label x:Name="SortLabel"
                                       Text="Sort"
                                       FontSize="12"
                                       TextColor="#CCCCDD"
                                       VerticalOptions="Center" />
                                <Border x:Name="SortDot"
                                        IsVisible="False"
                                        WidthRequest="6"
                                        HeightRequest="6"
                                        BackgroundColor="#D0BCFF"
                                        StrokeShape="RoundRectangle 3"
                                        Stroke="Transparent"
                                        VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Border>
                    </HorizontalStackLayout>

                    <!-- Expanded: search bar + filter + sort on one row -->
                    <Grid x:Name="ExpandedToolbar"
                          IsVisible="False"
                          ColumnDefinitions="Auto,*,Auto,Auto"
                          ColumnSpacing="8"
                          VerticalOptions="Center">
                        <!-- Search icon prefix -->
                        <Border BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                WidthRequest="36"
                                HeightRequest="36"
                                Padding="0">
                            <Label Text="&#xe8b6;"
                                   FontFamily="Material"
                                   FontSize="16"
                                   TextColor="#D0BCFF"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>

                        <!-- Search text field -->
                        <Border Grid.Column="1"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="#D0BCFF"
                                HeightRequest="36"
                                Padding="0"
                                VerticalOptions="Center">
                            <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center" Margin="10,0,4,0">
                                <Entry x:Name="SearchEntry"
                                       Placeholder="Search products..."
                                       PlaceholderColor="#666688"
                                       TextColor="White"
                                       FontSize="12"
                                       BackgroundColor="Transparent"
                                       Text="{Binding SearchText, Mode=TwoWay}"
                                       VerticalOptions="Center" />
                                <Border Grid.Column="1"
                                        x:Name="SearchDismiss"
                                        BackgroundColor="Transparent"
                                        StrokeShape="RoundRectangle 12"
                                        Stroke="Transparent"
                                        WidthRequest="24"
                                        HeightRequest="24"
                                        VerticalOptions="Center">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnSearchDismissTapped" />
                                    </Border.GestureRecognizers>
                                    <Label Text="&#xe5cd;"
                                           FontFamily="Material"
                                           FontSize="14"
                                           TextColor="#8888AA"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
                                </Border>
                            </Grid>
                        </Border>

                        <!-- Filter pill (expanded copy, same appearance) -->
                        <Border Grid.Column="2"
                                x:Name="FilterPillExpanded"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                HeightRequest="36"
                                Padding="12,0"
                                VerticalOptions="Center">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnFilterPillTapped" />
                            </Border.GestureRecognizers>
                            <HorizontalStackLayout Spacing="5" VerticalOptions="Center">
                                <Label Text="&#xe152;"
                                       FontFamily="Material"
                                       FontSize="13"
                                       TextColor="#CCCCDD" />
                                <Label x:Name="FilterLabelEx"
                                       Text="Filter"
                                       FontSize="11"
                                       TextColor="#CCCCDD" />
                            </HorizontalStackLayout>
                        </Border>

                        <!-- Sort pill (expanded copy) -->
                        <Border Grid.Column="3"
                                x:Name="SortPillExpanded"
                                BackgroundColor="#1E1E3A"
                                StrokeShape="RoundRectangle 18"
                                Stroke="Transparent"
                                HeightRequest="36"
                                Padding="12,0"
                                VerticalOptions="Center">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSortPillTapped" />
                            </Border.GestureRecognizers>
                            <HorizontalStackLayout Spacing="5" VerticalOptions="Center">
                                <Label Text="&#xe164;"
                                       FontFamily="Material"
                                       FontSize="13"
                                       TextColor="#CCCCDD" />
                                <Label x:Name="SortLabelEx"
                                       Text="Sort"
                                       FontSize="11"
                                       TextColor="#CCCCDD" />
                            </HorizontalStackLayout>
                        </Border>
                    </Grid>
                </Grid>

                <!-- Row 1: Filter / Sort segment panels -->
                <Grid Grid.Row="1" x:Name="ToolbarRow1" Margin="0,6,0,0">

                    <!-- Filter segment panel -->
                    <Border x:Name="FilterPanel"
                            IsVisible="False"
                            BackgroundColor="#14142E"
                            StrokeShape="RoundRectangle 12"
                            Stroke="#2A2E58"
                            Padding="4"
                            HeightRequest="40"
                            HorizontalOptions="Start">
                        <HorizontalStackLayout Spacing="3" VerticalOptions="Center">
                            <Border x:Name="FilterSegmentExpiring"
                                    BackgroundColor="#2A2E58"
                                    StrokeShape="RoundRectangle 9"
                                    Stroke="Transparent"
                                    Padding="14,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnFilterSegmentExpiringTapped" />
                                </Border.GestureRecognizers>
                                <Label x:Name="FilterLabelExpiring"
                                       Text="Expiring soon"
                                       FontSize="12"
                                       TextColor="White"
                                       VerticalOptions="Center" />
                            </Border>
                            <Border x:Name="FilterSegmentExpired"
                                    BackgroundColor="Transparent"
                                    StrokeShape="RoundRectangle 9"
                                    Stroke="Transparent"
                                    Padding="14,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnFilterSegmentExpiredTapped" />
                                </Border.GestureRecognizers>
                                <Label x:Name="FilterLabelExpired"
                                       Text="Expired"
                                       FontSize="12"
                                       TextColor="#8888AA"
                                       VerticalOptions="Center" />
                            </Border>
                            <Border x:Name="FilterSegmentAll"
                                    BackgroundColor="Transparent"
                                    StrokeShape="RoundRectangle 9"
                                    Stroke="Transparent"
                                    Padding="14,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnFilterSegmentAllTapped" />
                                </Border.GestureRecognizers>
                                <Label x:Name="FilterLabelAll"
                                       Text="All"
                                       FontSize="12"
                                       TextColor="#8888AA"
                                       VerticalOptions="Center" />
                            </Border>
                        </HorizontalStackLayout>
                    </Border>

                    <!-- Sort segment panel -->
                    <Border x:Name="SortPanel"
                            IsVisible="False"
                            BackgroundColor="#14142E"
                            StrokeShape="RoundRectangle 12"
                            Stroke="#2A2E58"
                            Padding="4"
                            HeightRequest="40"
                            HorizontalOptions="End">
                        <HorizontalStackLayout Spacing="3" VerticalOptions="Center">
                            <Border x:Name="SortSegmentAZ"
                                    BackgroundColor="#2A2E58"
                                    StrokeShape="RoundRectangle 9"
                                    Stroke="Transparent"
                                    Padding="14,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnSortSegmentAZTapped" />
                                </Border.GestureRecognizers>
                                <Label x:Name="SortLabelAZ"
                                       Text="A-Z"
                                       FontSize="12"
                                       TextColor="White"
                                       VerticalOptions="Center" />
                            </Border>
                            <Border x:Name="SortSegmentExpiry"
                                    BackgroundColor="Transparent"
                                    StrokeShape="RoundRectangle 9"
                                    Stroke="Transparent"
                                    Padding="14,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnSortSegmentExpiryTapped" />
                                </Border.GestureRecognizers>
                                <Label x:Name="SortLabelExpiry"
                                       Text="By expiry"
                                       FontSize="12"
                                       TextColor="#8888AA"
                                       VerticalOptions="Center" />
                            </Border>
                        </HorizontalStackLayout>
                    </Border>
                </Grid>
            </Grid>

            <!-- Add item card (tonal container wrapping SfAutocomplete) -->
            <Border BackgroundColor="#202448"
                    StrokeShape="RoundRectangle 14"
                    Stroke="Transparent"
                    HeightRequest="48"
                    Padding="12,0"
                    Margin="0,0,0,5">
                <Grid ColumnDefinitions="Auto,*" VerticalOptions="Center">
                    <!-- + icon container -->
                    <Border BackgroundColor="#2A2E58"
                            StrokeShape="RoundRectangle 10"
                            Stroke="Transparent"
                            WidthRequest="32"
                            HeightRequest="32"
                            VerticalOptions="Center">
                        <Label Text="+"
                               FontSize="18"
                               TextColor="#D0BCFF"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Border>

                    <!-- Autocomplete input -->
                    <editors:SfAutocomplete Grid.Column="1"
                                             x:Name="addItemAutocomplete"
                                             ShowBorder="False"
                                             Stroke="Transparent"
                                             Completed="SfAutocomplete_Completed"
                                             DisplayMemberPath="Name"
                                             ItemsSource="{Binding GrocerySuggestions}"
                                             MaximumSuggestion="3"
                                             MinimumPrefixCharacters="1"
                                             Placeholder="Add item"
                                             SelectedItem="{Binding SelectedGrocerySuggestion}"
                                             Text="{Binding NewItemName, Mode=TwoWay}"
                                             TextMemberPath="Name"
                                             Margin="10,0,0,0"
                                             VerticalOptions="Center">
                        <editors:SfAutocomplete.FilterBehavior>
                            <behaviours:SearchBehavior />
                        </editors:SfAutocomplete.FilterBehavior>
                    </editors:SfAutocomplete>
                </Grid>
            </Border>
        </StackLayout>

        <Entry
            x:Name="hiddenEntry"
            IsVisible="False"
            Text="{Binding NewItemName, Mode=TwoWay}" />

        <pulltoRefresh:SfPullToRefresh
            x:Name="pullToRefresh"
            Grid.Row="1"
            IsRefreshing="False"
            PullingThreshold="150"
            RefreshViewHeight="50"
            RefreshViewThreshold="30"
            RefreshViewWidth="50"
            Refreshing="pullToRefresh_Refreshing"
            TransitionMode="SlideOnTop">
            <pulltoRefresh:SfPullToRefresh.PullableContent>
                <Grid x:Name="mainGrid">
                    <sf:SfListView
                        x:Name="listView"
                        x:DataType="vm:ProductsViewModel"
                        AutoFitMode="DynamicHeight"
                        IsStickyGroupHeader="True"
                        ItemsSource="{Binding GroupedProducts}"
                        SelectionMode="None">
                        <sf:SfListView.ItemTemplate>
                            <DataTemplate x:DataType="models:ListViewFoodCategory">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- Category group header (M3 style) -->
                                    <Grid Grid.Row="0" Padding="4,2,4,6" Margin="0,12,0,0">
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding ToggleExpandCommand}" />
                                        </Grid.GestureRecognizers>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Label
                                            Grid.Column="0"
                                            FontSize="13"
                                            FontAttributes="Bold"
                                            CharacterSpacing="0.5"
                                            TextColor="White"
                                            Text="{Binding FoodCategory}" />
                                        <Label
                                            Grid.Column="1"
                                            Margin="8,0,0,0"
                                            FontSize="11"
                                            TextColor="#8888AA"
                                            VerticalOptions="Center"
                                            Text="{Binding FoodMenuCollection.Count, StringFormat='· {0} items'}" />
                                        <Label
                                            Grid.Column="3"
                                            FontFamily="Material"
                                            FontSize="12"
                                            TextColor="#666688"
                                            Text="{Binding IsExpanded, Converter={StaticResource ExpandCollapseIconConverter}}" />
                                    </Grid>

                                    <!-- Product list (visible when expanded) -->
                                    <StackLayout
                                        Grid.Row="1"
                                        BindableLayout.ItemsSource="{Binding FoodMenuCollection}"
                                        IsVisible="{Binding IsExpanded}">
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate x:DataType="models:Product">
                                                <Border
                                                    BackgroundColor="#171A35"
                                                    StrokeShape="RoundRectangle 14"
                                                    Stroke="Transparent"
                                                    Padding="14,0"
                                                    Margin="0,0,0,8"
                                                    HeightRequest="48">
                                                    <Border.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding ToggleSelectCommand}" />
                                                    </Border.GestureRecognizers>
                                                    <Grid ColumnDefinitions="*,Auto,Auto,Auto" VerticalOptions="Center">
                                                        <!-- Product name -->
                                                        <Label
                                                            Grid.Column="0"
                                                            FontFamily="Roboto-Regular"
                                                            FontSize="14"
                                                            TextColor="White"
                                                            VerticalOptions="Center"
                                                            LineBreakMode="TailTruncation"
                                                            Text="{Binding Name}" />

                                                        <!-- Frozen icon (if frozen) -->
                                                        <Label
                                                            Grid.Column="1"
                                                            FontFamily="Material"
                                                            FontSize="16"
                                                            TextColor="#8888AA"
                                                            VerticalOptions="Center"
                                                            IsVisible="{Binding ShowFrozenIcon}"
                                                            Text="ac_unit" />

                                                        <!-- Expiry badge -->
                                                        <Border
                                                            Grid.Column="2"
                                                            Margin="6,0,0,0"
                                                            StrokeShape="RoundRectangle 8"
                                                            Stroke="Transparent"
                                                            Padding="12,5"
                                                            IsVisible="{Binding ShowExpiryBadge}"
                                                            BackgroundColor="{Binding ExpiryColor}"
                                                            VerticalOptions="Center">
                                                            <Label
                                                                FontSize="12"
                                                                FontAttributes="Bold"
                                                                TextColor="{Binding ExpiryTextColor}"
                                                                Text="{Binding ExpiryDisplayText}" />
                                                        </Border>

                                                        <!-- Edit button -->
                                                        <Border
                                                            Grid.Column="3"
                                                            Margin="6,0,0,0"
                                                            BackgroundColor="#1E1E3A"
                                                            StrokeShape="RoundRectangle 16"
                                                            Stroke="Transparent"
                                                            WidthRequest="32"
                                                            HeightRequest="32"
                                                            VerticalOptions="Center">
                                                            <Border.GestureRecognizers>
                                                                <TapGestureRecognizer Tapped="OnEditProductTapped" />
                                                            </Border.GestureRecognizers>
                                                            <Label
                                                                Text="&#xe3c9;"
                                                                FontFamily="Material"
                                                                FontSize="14"
                                                                TextColor="#CCCCDD"
                                                                HorizontalOptions="Center"
                                                                VerticalOptions="Center" />
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </StackLayout>
                                </Grid>
                            </DataTemplate>
                        </sf:SfListView.ItemTemplate>
                    </sf:SfListView>

                    <!-- Floating barcode FAB -->
                    <Border x:Name="BarcodeFab"
                            BackgroundColor="#D0BCFF"
                            StrokeShape="RoundRectangle 28"
                            Stroke="Transparent"
                            WidthRequest="56"
                            HeightRequest="56"
                            VerticalOptions="End"
                            HorizontalOptions="End"
                            Margin="0,0,16,16"
                            InputTransparent="False"
                            ZIndex="100">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BarcodeCommand}" />
                        </Border.GestureRecognizers>
                        <Label Text="&#xef39;"
                               FontFamily="Material"
                               FontSize="22"
                               TextColor="#0D0D2B"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Border>
                </Grid>
            </pulltoRefresh:SfPullToRefresh.PullableContent>
        </pulltoRefresh:SfPullToRefresh>
        </Grid>

        <!-- Floating Bottom Bar -->
        <controls:FloatingBottomBar Grid.Row="1" />
    </Grid>
</ContentPage>
