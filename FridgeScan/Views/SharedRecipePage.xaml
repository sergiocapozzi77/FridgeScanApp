<ContentPage
    x:Class="FridgeScan.Views.SharedRecipePage"
    x:Name="thisPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:button="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
    xmlns:sfBusy="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:models="clr-namespace:FridgeScan.Models"
    xmlns:viewmodel="clr-namespace:FridgeScan.ViewModels">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="10">
            <Label
                FontAttributes="Bold"
                FontSize="24"
                Text="{Binding PageTitle}" />

            <Label
                FontSize="14"
                Text="{Binding SharedUrl}"
                TextColor="Gray"
                LineBreakMode="TailTruncation" />

            <BoxView HeightRequest="1" Color="LightGray" />
        </VerticalStackLayout>

        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="12">

                <!-- Loading -->
                <VerticalStackLayout IsVisible="{Binding IsLoading}" Spacing="8" VerticalOptions="Center" HorizontalOptions="Center" Padding="0,40">
                    <sfBusy:SfBusyIndicator
                        AnimationType="CircularMaterial"
                        IsRunning="{Binding IsLoading}"
                        HorizontalOptions="Center"
                        HeightRequest="60"
                        WidthRequest="60" />
                    <Label Text="Importing recipe..." HorizontalOptions="Center" TextColor="Gray" />
                </VerticalStackLayout>

                <!-- Imported recipe -->
                <VerticalStackLayout IsVisible="{Binding HasRecipe}" Spacing="12">

                    <Image
                        Source="{Binding ImportedRecipe.ImageUrl}"
                        Aspect="AspectFill"
                        HeightRequest="200" />

                    <HorizontalStackLayout Spacing="16">
                        <Label FontSize="14" Text="{Binding ImportedRecipe.PrepTime, StringFormat='Prep: {0}'}" TextColor="Gray" IsVisible="{Binding HasPrepTime}" />
                        <Label FontSize="14" Text="{Binding ImportedRecipe.CookTime, StringFormat='Cook: {0}'}" TextColor="Gray" IsVisible="{Binding HasCookTime}" />
                        <Label FontSize="14" Text="{Binding ImportedRecipe.Difficulty}" TextColor="Gray" />
                        <Label FontSize="14" Text="{Binding ImportedRecipe.Serving, StringFormat='Serves: {0}'}" TextColor="Gray" IsVisible="{Binding HasServing}" />
                    </HorizontalStackLayout>

                    <Label FontSize="16" FontAttributes="Bold" Text="Ingredients" Margin="0,8,0,0" />
                    <CollectionView ItemsSource="{Binding ImportedRecipe.Ingredients}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Label FontSize="14" Text="{Binding .}" Padding="4,2" />
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <Label FontSize="16" FontAttributes="Bold" Text="Method" Margin="0,8,0,0" />
                    <CollectionView ItemsSource="{Binding ImportedRecipe.MethodSteps}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Grid ColumnDefinitions="Auto,*" Padding="4,4" ColumnSpacing="8">
                                    <Label Grid.Column="0" FontSize="14" Text="&#x2022;" VerticalOptions="Start" />
                                    <Label Grid.Column="1" FontSize="14" Text="{Binding .}" LineBreakMode="WordWrap" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <!-- Error state -->
                <Label
                    IsVisible="{Binding HasError}"
                    FontSize="14"
                    Text="{Binding ErrorMessage}"
                    TextColor="Gray"
                    HorizontalOptions="Center"
                    Padding="0,40" />
                <Label
                    IsVisible="{Binding HasError}"
                    FontSize="12"
                    Text="Check the debug log for more details."
                    TextColor="DimGray"
                    HorizontalOptions="Center" />

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom bar -->
        <Grid Grid.Row="2" Padding="16" BackgroundColor="#1e1e1e">
            <!-- Default buttons -->
            <HorizontalStackLayout Spacing="12" HorizontalOptions="Center" IsVisible="{Binding ShowDefaultButtons}">
                <button:SfButton
                    Command="{Binding CloseCommand}"
                    CornerRadius="6"
                    HeightRequest="44"
                    HorizontalOptions="Center"
                    Text="Close"
                    WidthRequest="120" />
                <button:SfButton
                    Command="{Binding ShowCookbookPanelCommand}"
                    CornerRadius="6"
                    HeightRequest="44"
                    HorizontalOptions="Center"
                    Text="Save to Cookbook"
                    IsVisible="{Binding HasRecipe}"
                    BackgroundColor="#4a90d9"
                    WidthRequest="160" />
            </HorizontalStackLayout>

            <!-- Cookbook selection panel -->
            <VerticalStackLayout Spacing="12" IsVisible="{Binding IsCookbookPanelVisible}">
                <Label Text="Save to Cookbook" FontSize="15" FontAttributes="Bold" />
                <CollectionView ItemsSource="{Binding AllCookbooks}"
                                SelectionMode="Single"
                                SelectedItem="{Binding SelectedCookbook}"
                                HeightRequest="44">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeShape="RoundRectangle 20"
                                    Stroke="#555"
                                    StrokeThickness="1"
                                    BackgroundColor="Transparent"
                                    Padding="16,8"
                                    HeightRequest="36">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="BackgroundColor" Value="#4a90d9" />
                                        <Setter Property="Stroke" Value="#4a90d9" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Label Text="{Binding Name}" FontSize="13"
                                       VerticalOptions="Center" HorizontalOptions="Center" />
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <Frame Padding="10,8" CornerRadius="20" BorderColor="#555"
                       BackgroundColor="Transparent" HasShadow="False" HorizontalOptions="Start">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CreateAndAddCookbookCommand}" />
                    </Frame.GestureRecognizers>
                    <Label Text="+ New Cookbook" FontSize="13" TextColor="Gray" />
                </Frame>
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <button:SfButton
                        Grid.Column="0"
                        Command="{Binding HideCookbookPanelCommand}"
                        CornerRadius="6"
                        HeightRequest="44"
                        Text="Cancel" />
                    <button:SfButton
                        Grid.Column="1"
                        Command="{Binding SaveCommand}"
                        CornerRadius="6"
                        HeightRequest="44"
                        Text="Save Recipe"/>
                </Grid>
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>
